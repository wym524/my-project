# 图标说明

把你的应用图标放到这里，命名为 **`icon.png`（256×256 像素 PNG 格式）。

## 快速生成一个图标

你可以在 PowerPoint/画图工具：
1. 画一个蓝色方块，中间放"雅思"两个字
2. 导出为 256×256 PNG
3. 命名为 icon.png 放到这里

或者使用在线工具（如 iconverticons.com 等）一键生成。

## 没有也没关系

如果没有图标文件，打包时 electron-builder 会使用默认 Electron 图标，一样能正常安装。
